class Coach {
  Coach(this.id, this.nome, this.cognome, this.email, this.permesso,
      this.palestra);

  String id;
  String nome;
  String cognome;
  String email;
  bool? permesso;
  String? palestra;
}
